# 指定了使用 Docker Compose 檔案格式的版本
version: "3"
services:
  # 服務名稱
  web:
    build: . # 當前位置
    ports:
      - "5000:5000"
    volumes:
      - .:/app # 🤔這行設定了一個卷映射

# 執行命令
# docker compose up
